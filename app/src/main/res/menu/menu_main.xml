<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.luck_art.lamzone.MainActivity"
    tools:drawable="@drawable/filter_icone">
    <item
        android:id="@+id/filtre"
        android:checkable="true"
        android:enabled="true"
        android:icon="@drawable/filter_icone"
        android:title="@string/filtres"
        android:visible="true"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/hour_filter"
                android:orderInCategory="100"
                android:title="@string/Heures"
                android:visible="true"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/place_filter"
                android:orderInCategory="100"
                android:title="@string/Salles"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/clear_filter"
                android:orderInCategory="100"
                android:title="@string/Clear"
                android:visible="true"
                app:showAsAction="ifRoom" />
        </menu>
    </item>
</menu>